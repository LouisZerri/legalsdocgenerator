services:
  # PHP 8.3 + Apache
  app:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: legaldocs_app
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
      - ./docker/apache/vhost.conf:/etc/apache2/sites-available/000-default.conf
    depends_on:
      - database
      - redis
      - rabbitmq
    environment:
      - APP_ENV=dev
      - DATABASE_URL=postgresql://legaldocs:legaldocs@database:5432/legaldocs?serverVersion=16&charset=utf8
      - REDIS_URL=redis://redis:6379
      - MESSENGER_TRANSPORT_DSN=amqp://guest:guest@rabbitmq:5672/%2f/messages
    networks:
      - legaldocs_network

  # PostgreSQL + pgvector
  database:
    image: pgvector/pgvector:pg16
    container_name: legaldocs_db
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: legaldocs
      POSTGRES_USER: legaldocs
      POSTGRES_PASSWORD: legaldocs
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - legaldocs_network

  # Redis
  redis:
    image: redis:7-alpine
    container_name: legaldocs_redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - legaldocs_network

  # RabbitMQ
  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: legaldocs_rabbitmq
    restart: unless-stopped
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - legaldocs_network

  # Ollama pour l'IA
  ollama:
    image: ollama/ollama:latest
    container_name: legaldocs_ollama
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - legaldocs_network

  # Worker Messenger
  worker:
    build:
      context: .
      dockerfile: docker/php/Dockerfile
    container_name: legaldocs_worker
    restart: unless-stopped
    command: php bin/console messenger:consume async --time-limit=3600 --memory-limit=256M
    volumes:
      - ./:/var/www/html
    depends_on:
      - database
      - redis
      - rabbitmq
    environment:
      - APP_ENV=dev
      - DATABASE_URL=postgresql://legaldocs:legaldocs@database:5432/legaldocs?serverVersion=16&charset=utf8
      - REDIS_URL=redis://redis:6379
      - MESSENGER_TRANSPORT_DSN=amqp://guest:guest@rabbitmq:5672/%2f/messages
      - OLLAMA_HOST=ollama
    networks:
      - legaldocs_network
      
volumes:
  postgres_data:
  redis_data:
  rabbitmq_data:
  ollama_data:

networks:
  legaldocs_network:
    driver: bridge